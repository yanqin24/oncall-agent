## 1. 应用程序基础

- [x] 1.1 将 Vue 路由器、Pinia 和图标依赖项添加到前端工作区。
- [x] 1.2 为类型化 HTTP 信封处理、安全的错误响应失败、授权头和类型化 SSE 解析添加失败的传输测试。
- [x] 1.3 实现仅由 `@agent-py/api-contracts` 支持的可重用类型化 API 和 SSE 客户端。
- [x] 1.4 为认证恢复、认证重定向、无效令牌清除和反馈状态添加失败的状态和路由测试。
- [x] 1.5 实现 Pinia 认证和反馈存储以及路由元数据和守卫。

## 2. 响应式 shell 和共享组件

- [x] 2.1 为加载、空状态、错误和工作区导航展示添加失败的组件测试。
- [x] 2.2 实现可重用的加载、空状态、错误状态、反馈和导航组件，并带有可访问的标签。
- [x] 2.3 用公共身份验证路由、经过认证的响应式工作区布局和路由占位符替换临时根页面。
- [x] 2.4 保留低级类型客户端，不使用浏览器存储作为聊天、文档或诊断数据的来源。

## 3. 验证和交付

- [x] 3.1 运行前端类型检查、单元测试和生产构建。
- [x] 3.2 运行共享 API-contract 检查和 OpenSpec 验证。
- [x] 3.3 在本地启动后端和前端，在浏览器中测试公共和已认证的路由导航，并验证反馈行为。
- [x] 3.4 归档完成的 OpenSpec 变更，提交实现，并通过 `main` 通过 SSH 端口 443 推送。
