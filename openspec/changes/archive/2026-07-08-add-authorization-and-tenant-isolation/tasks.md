## 1. 合同和失败的测试

- [x] 1.1 为作用域内存仓库、跨tenant拒绝和迁移列/索引添加失败的后端测试。
- [x] 1.2 为作用域受保护数据和统一禁止错误添加失败的后端API测试。
- [x] 1.3 为`AUTH_FORBIDDEN`、OpenAPI 403响应和向量元数据/过滤器契约添加失败的API契约测试。
- [x] 1.4 为经过身份验证的作用域数据加载和注销时清除受保护数据添加失败的前端测试。

## 2. 后端作用域实现

- [x] 2.1 添加 Alembic 迁移以及 SQLAlchemy owner 范围字段/索引，用于作用域内存表。
- [x] 2.2 更新内存仓库记录、协议和 SQLite 实现，以要求并过滤 `owner_user_id`。
- [x] 2.3 为 Milvus ownership 合同添加向量元数据/过滤帮助模块。
- [x] 2.4 添加统一的 `AUTH_FORBIDDEN` 响应处理和受保护端点作用域检查。

## 3. 前端和合约

- [x] 3.1 更新共享的 API 合同和 OpenAPI 模式，以处理禁止错误和作用域受保护操作。
- [x] 3.2 添加前端受保护数据客户端/状态，以便可见的受保护数据与当前身份验证状态相关联。
- [x] 3.3 更新面向 user 的已认证外壳，通过当前 user 清除并重新加载受保护数据。

## 4. 验证与归档

- [x] 4.1 运行后端、前端、api-contracts、迁移和OpenSpec验证检查。
- [x] 4.2 将增量规范同步到主规范并归档更改。
- [x] 4.3 提交完成的工作并通过`main`通过SSH端口443推送。
