## 1. 共享合约

- [x] 1.1 向`packages/api-contracts`添加共享流式聊天请求/结果类型和SSE解析预期。
- [x] 1.2 更新OpenAPI聊天路径以包含受保护的SSE流式传输端点。
- [x] 1.3 为聊天流DTO、SSE工具生命周期状态和OpenAPI覆盖范围添加契约测试。

## 2. 后端流式传输 Agent

- [x] 2.1 为经过身份验证的流式聊天持久化添加后端测试，SSE 事件顺序，跨tenant拒绝，以及安全错误事件。
- [x] 2.2 添加一个聊天流服务，用于持久化user消息，通过可注入的运行器运行Agent，发出共享的SSE事件，并持久化最终的助手消息。
- [x] 2.3 使用`create_agent`、配置的LLM提供程序和请求范围的工具，添加一个LangChain Agent运行器。
- [x] 2.4 将知识检索功能作为绑定到当前user的tenant作用域的LangChain工具公开。
- [x] 2.5 为经过身份验证的FastAPI SSE端点添加流式聊天发送，并将其连接到服务。

## 3. 前端流式聊天

- [x] 3.1 为流式聊天客户端的事件解析和状态更新添加前端测试 SSE。
- [x] 3.2 添加一个前端流式聊天客户端方法，用于发送带有承载认证的请求并解析共享的 SSE 事件。
- [x] 3.3 更新聊天状态和用户界面，使得发送消息使用流式端点，渲染助理的增量/工具/参考进度，并从后端支持的状态中协调完成。

## 4. 验证和归档

- [x] 4.1 运行后端、前端、包和OpenSpec验证命令。
- [x] 4.2 将增量规范同步到主规范中，并归档OpenSpec更改。
- [x] 4.3 提交已完成的更改，并通过端口443经由SSH推送`main`。
