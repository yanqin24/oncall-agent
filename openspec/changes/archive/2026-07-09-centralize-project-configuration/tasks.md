## 1. 项目配置文件

- [x] 1.1 为直接本地启动添加跟踪根项目配置，使用具体的开发值。
- [x] 1.2 为Docker Compose启动添加跟踪根项目配置，使用服务内部的地址和路径。
- [x] 1.3 移除旧的`.env.example`、基础设施`.env`、前端`VITE_*`和后端提供者JSON配置源。

## 2. 运行时配置加载

- [x] 2.1 添加后端项目配置加载器并通过FastAPI启动传递配置路径。
- [x] 2.2 重构LLM、内存数据库、向量存储和Alembic配置，以读取跟踪的项目配置文件。
- [x] 2.3 重构前端API配置，以读取跟踪的根项目配置JSON。
- [x] 2.4 重构Dockerfile和Compose启动，以包含项目配置并避免本地env文件依赖。

## 3. 文档和规范

- [x] 3.1 更新 README、后端文档、基础设施文档、AGENTS 和基础文档，以跟踪配置。
- [x] 3.2 更新 OpenSpec 的项目基础要求、Qwen 提供者、Compose 启动、内存仓库和 Milvus 向量存储的要求。
- [x] 3.3 更新后端和前端测试，以支持仅配置文件的应用程序设置。

## 4. 验证和归档

- [x] 4.1 运行后端的代码检查、类型检查和测试验证。
- [x] 4.2 运行前端的类型检查、测试和构建验证。
- [x] 4.3 运行OpenSpec验证并归档更改。
- [x] 4.4 提交已完成的更改并通过端口 443 经 SSH 推送`main`。
