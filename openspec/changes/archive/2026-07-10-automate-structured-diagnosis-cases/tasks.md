## 1. 结构化持久化边界

- [x] 1.1 为owner作用域的案例创建、列表、重复处理和失败任务排除添加失败的迁移/仓库测试。
- [x] 1.2 添加SQLite案例模型、Alembic迁移、记录和仓库接口/实现。
- [x] 1.3 添加一个案例持久化器，用于推导保守的结构化字段，创建关联文档/索引任务，并安排索引。

## 2. AIOps 自动创建和库

- [x] 2.1 为自动成功案例创建、不保留失败案例和AIOps隔离添加失败的AIOps服务/API测试。
- [x] 2.2 仅在报告成功后调用持久化器，并公开类型化的已认证案例列表API。
- [x] 2.3 为浏览当前user诊断案例和打开相关制品添加前端契约/客户端/存储/组件/测试。

## 3. 验证和交付

- [x] 3.1 运行后端检查、前端类型检查/测试/构建、API-合约检查、迁移和OpenSpec验证。
- [x] 3.2 启动本地应用程序并验证实际成功的诊断是否自动创建一个可在案例库中看到的索引案例。
- [x] 3.3 同步完成的规范，归档OpenSpec变更，提交并通过`main`端口443推送SSH。
