## 1. 契约与后端上传规则

- [x] 1.1 更新共享文档上传契约：仅支持 `.md`、`.pdf`，并让非固定分片配置不要求 `maxCharacters`/`overlapCharacters`。
- [x] 1.2 更新后端上传策略和错误消息，明确文件大小、格式、Markdown MIME 兼容和无文本 PDF 的恢复提示。
- [x] 1.3 接入 `langchain-text-splitters` 与 `pypdf`，实现 Markdown/PDF 可索引文本提取和三种分片策略。
- [x] 1.4 更新后端测试，覆盖 md/pdf 限制、Skill 上传规范、三种分片策略和非固定策略参数。

## 2. 聊天配置侧边栏

- [x] 2.1 将系统提示词配置拆成“对话系统提示词设置”侧边栏，支持创建、保存、删除、单选使用和折叠/展开。
- [x] 2.2 将 Skill 配置拆成“skill设置”侧边栏，支持上传、删除、多选使用和规范展示。
- [x] 2.3 改进前端错误提示，上传 Skill 或知识文档失败时展示具体原因与修复方式。
- [x] 2.4 更新聊天配置相关前端组件测试。

## 3. 知识库上传体验

- [x] 3.1 更新知识库上传 UI：只展示 Markdown/PDF；仅 `fixed-character` 展示最大字符和 overlap 输入。
- [x] 3.2 更新知识库 store/client 默认分片配置，确保非固定策略只发送 `strategy`。
- [x] 3.3 更新知识库组件测试，覆盖本地格式校验和非固定策略 UI。

## 4. AIOps 页面重构

- [x] 4.1 重构 `/aiops` 页面布局为中文三栏/单栏响应式诊断控制台。
- [x] 4.2 强化等待中、执行中、成功、失败状态展示和证据/报告可扫描区域。
- [x] 4.3 更新 AIOps 相关测试或快照断言，确保核心中文区域存在。

## 5. 验证、归档与提交

- [x] 5.1 运行后端 lint、类型检查和测试。
- [x] 5.2 运行前端 typecheck、测试和 build。
- [x] 5.3 运行 OpenSpec validate、归档变更并确认归档后规格通过。
- [x] 5.4 提交并通过 SSH 443 推送到 `main`。
