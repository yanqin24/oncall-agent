## 1. 共享中文设计系统

- [x] 1.1 为中文工作区标签和标准化的异步生命周期标签添加失败的前端测试。
- [x] 1.2 创建共享的异步状态映射和徽章组件，支持可访问的等待、运行中、已完成和失败状态。
- [x] 1.3 用平静的中文AI工作区系统、焦点状态、减少运动规则和响应式约束替换全局视觉标记和基础样式。

## 2. 应用程序外壳和身份验证

- [x] 2.1 为中文导航、账户控制、路由状态和窄屏导航添加失败的组件测试。
- [x] 2.2 使用现有路由和 Lucide 图标，将已认证的工作区外壳和导航重构为类似 ChatGPT 的聚焦布局。
- [x] 2.3 将登录和注册界面重构为中文、可访问且具备任务反馈意识的页面，而不更改身份验证流程。

## 3. 聊天工作区

- [x] 3.1 为中文作曲家、对话状态、流式传输状态、工具活动和引用添加失败的聊天组件测试。
- [x] 3.2 将会话、转录本、作曲家、引用详细信息和工具审计展示重构为聚焦的中文对话体验。
- [x] 3.3 保持桌面对话外壳的视口边界，并且仅让转录本区域在长消息历史中滚动。

## 4. 知识工作区

- [x] 4.1 为中文上传、文档生命周期状态、索引重试、详情和删除确认添加失败的组件测试。
- [x] 4.2 使用共享的异步状态组件重构知识库选择、上传、文档列表、文档详情和生命周期展示。

## 5. AIOps 工作区

- [x] 5.1 为中文诊断条目、警报刷新、实时时间线、证据、历史记录以及等待/运行中/已完成/失败状态添加失败的组件测试。
- [x] 5.2 使用统一的操作工作区模式对 AIOps 条目、警报列表、历史记录、实时时间线、案例库、报告和证据链表面进行重构。

## 6. 验证与交付

- [x] 6.1 运行前端类型检查、单元测试、生产构建、后端回归检查以及 OpenSpec 验证。
- [x] 6.2 启动本地服务，并通过浏览器交互在桌面和窄屏移动视口上验证重新构建的UI，使用真实的聊天、知识、警报和 AIOps 数据。
- [x] 6.3 归档已验证的 OpenSpec 更改，提交实现，并通过 SSH 端口 443 推送 `main`。
