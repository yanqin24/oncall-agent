## 1. 类型化诊断边界

- [x] 1.1 为诊断创建、类型化 SSE 重新协调、证据链加载、历史选择和失败运行添加失败的前端测试。
- [x] 1.2 为任务创建、历史记录、证据链和 SSE 执行实现专用的类型化 AIOps 客户端。
- [x] 1.3 实现一个专注的 Pinia AIOps 存储，保留后端拥有的任务状态，并仅从共享的 SSE 事件生成实时事件投影。

## 2. 操作诊断工作区

- [x] 2.1 为事件条目验证、任务历史、实时阶段/工具/源/报告显示、证据链接和响应状态添加失败的组件测试。
- [x] 2.2 将 AIOps 占位符替换为事件条目、诊断历史、实时活动、报告和证据链组件。
- [x] 2.3 将退出登录和路由卸载清理连接起来，以清除可见的诊断状态而不删除持久化任务。

## 3. 验证和交付

- [x] 3.1 启动本地应用程序并验证浏览器中的真实 MCP 支持的 AIOps 诊断流、持久化报告和证据链。
- [x] 3.2 运行前端类型检查、测试、生产构建、共享合约检查、后端检查和 OpenSpec 验证。
- [x] 3.3 同步完成的规范，归档 OpenSpec 更改，提交实现，并通过 `main` 通过 SSH 端口 443 推送。
