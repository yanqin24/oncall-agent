## 1. 持久化和合约

- [x] 1.1 添加后端仓库记录/协议、SQLAlchemy 模型、Alembic 迁移和 SQLite 实现，用于 tenant 范围内的文档索引任务。
- [x] 1.2 添加共享的 API 合同 DTOs/OpenAPI 模式和路径，用于文档索引任务的创建、状态和重试。

## 2. 索引核心

- [x] 2.1 使用配置的嵌入模型值，添加一个 OpenAI-compatible 嵌入提供者抽象。
- [x] 2.2 实现确定性的文档 chunking 和文档索引服务，包括任务状态转换、失败原因、重试链接和作用域内的 Milvus 写入。
- [x] 2.3 扩展文档上传元数据，包含可索引文本，并确保 Milvus 向量插入仍由 tenant 拥有。

## 3. API 和前端

- [x] 3.1 添加受保护的后端 API 路由，用于触发、读取和重试文档索引任务，而不会阻塞请求完成。
- [x] 3.2 添加前端客户端/状态/UI 控制项，用于重建文档索引并刷新可见的文档/任务状态。

## 4. 验证和归档

- [x] 4.1 运行后端测试、代码风格检查、pyright、合约测试/类型检查、前端测试/构建，以及 OpenSpec 验证。
- [x] 4.2 同步规范，归档更改，提交并推送到 main 通过 SSH 端口 443。
