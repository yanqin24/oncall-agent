## 1. 共享合约

- [x] 1.1 向`packages/api-contracts`添加检索工具输入、过滤器、命中、引用和输出的DTO。
- [x] 1.2 扩展检索DTO导出的契约测试，空结果和SSE引用兼容性。

## 2. 后端检索工具

- [x] 2.1 为检索成功、空结果、未授权的过滤器、无效输入和安全基础设施故障添加失败的后端测试。
- [x] 2.2 使用嵌入模型和tenant作用域的向量存储搜索边界实现一个后端知识检索工具服务。
- [x] 2.3 导出检索工具模块，并保持聊天/会话创建不调用检索作为固定的前置步骤。

## 3. 集成验证

- [x] 3.1 添加或更新后端 API 测试，证明检索是 tenant 范围的，并且只有在代理/工具边界调用时才会被调用。
- [x] 3.2 运行后端、前端和合约质量检查。

## 4. OpenSpec 完成

- [x] 4.1 将增量规范同步到主规范中。
- [x] 4.2 验证并归档已完成的 OpenSpec 变更。
