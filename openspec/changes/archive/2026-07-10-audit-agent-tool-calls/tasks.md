## 1. 持久化和仓库边界

- [x] 1.1 添加通用的 tenant 范围的工具调用审计 SQLAlchemy 模型和 Alembic 迁移，以及带有聊天或诊断父级关联和查询索引的内容。
- [x] 1.2 定义通用的审计记录和仓库协议，然后实现 SQLite 的创建、最终化和范围查询操作。
- [x] 1.3 为聊天/诊断关联、生命周期最终化、持续时间以及跨 tenant 拒绝添加后端仓库和迁移测试。

## 2. 聊天和 API 集成

- [x] 2.1 通过审计存储库持久化聊天 Agent 工具生命周期事件，而不会中断 SSE 输出，使用有界结果和错误摘要。
- [x] 2.2 添加经过身份验证的聊天会话工具调用审计端点和共享后端有效负载映射。
- [x] 2.3 扩展共享 TypeScript/OpenAPI 合同和审计集合的合同测试。
- [x] 2.4 为成功、失败和 owner 范围的聊天审计访问添加流式处理和 API 测试。

## 3. 前端审计跟踪

- [x] 3.1 为加载会话工具调用审核和反映 SSE 生命周期更新添加类型保护数据客户端和状态支持。
- [x] 3.2 使用参数、结果/错误摘要、状态和持续时间渲染聊天会话审核跟踪。
- [x] 3.3 为历史审核加载和实时工具调用显示添加前端测试。

## 4. 验证和文档化

- [x] 4.1 运行后端代码检查、类型检查、迁移和测试。
- [x] 4.2 运行前端类型检查、测试和生产构建。
- [x] 4.3 验证 OpenSpec 工件并根据所有更改要求审查实现。
