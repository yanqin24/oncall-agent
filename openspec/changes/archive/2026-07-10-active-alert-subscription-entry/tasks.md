## 1. 实际的 alert-provider 边界

- [x] 1.1 添加完整的跟踪 Alertmanager 警报 API 配置和一个类型化、惰性 HTTP 提供者，用于对活动警报进行标准化。
- [x] 1.2 添加经过身份验证的活动警报 API 处理以及共享 TypeScript 合同，而不会暴露提供者内部或伪造的备用数据。
- [x] 1.3 为标准化的活动结果、提供者故障和身份验证添加后端测试。

## 2. AIOps 告警条目工作区

- [x] 2.1 添加带类型的前端客户端/存储状态以及用于加载、刷新、失败显示和选定警报诊断创建的测试。
- [x] 2.2 在 AIOps 工作区中添加一个响应式的活动警报列表，包含所有分诊字段和直接的“诊断”操作。

## 3. 验证和交付

- [x] 3.1 运行后端检查、前端类型检查/测试/构建、API-contract 检查和 OpenSpec 验证。
- [x] 3.2 启动本地应用程序，验证 active-alert 提供商的行为，并在配置的提供商可访问时从返回的警报中创建真实的 AIOps 诊断。
- [x] 3.3 同步完成的规范，归档 OpenSpec 的更改，提交实现，并通过 SSH 端口 443 推送 `main`。
