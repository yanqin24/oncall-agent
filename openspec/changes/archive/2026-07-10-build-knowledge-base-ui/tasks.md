## 1. 类型化知识数据边界

- [x] 1.1 为知识库加载、文档上传策略、索引任务进度、重试和删除行为添加失败的前端测试。
- [x] 1.2 实现一个共享传输的知识客户端，该客户端使用文档和索引契约，包括多部分上传。
- [x] 1.3 实现一个专注的 Pinia 知识存储，具有服务器支持的基础/文档状态和有限的活动任务轮询。

## 2. 知识工作区

- [x] 2.1 为上传验证、重复覆盖确认、索引状态/操作、元数据详情和响应式状态添加失败的组件测试。
- [x] 2.2 用可访问的基础选择、政策感知上传、文档列表、状态、详情和破坏性操作组件替换知识占位符路由。
- [x] 2.3 将注销和路由卸载清理连接起来，以清除可见状态并停止活动任务轮询。

## 3. 验证和交付

- [x] 3.1 启动本地后端/前端并验证真实支持的文档上传、索引生命周期和浏览器中的刷新。
- [x] 3.2 运行前端类型检查、测试、生产构建、共享契约检查以及 OpenSpec 验证。
- [x] 3.3 同步完成的规范，归档 OpenSpec 更改，提交实现，并通过 `main` 通过 SSH 端口 443 推送。
