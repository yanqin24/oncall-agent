## 1. 合同和持久化

- [x] 1.1 扩展共享的 AIOps API 和 SSE 合同，用于诊断流状态、报告和证据感知结果。
- [x] 1.2 通过仓库边界添加 owner 范围内的诊断任务状态转换和结果更新。

## 2. 诊断流程

- [x] 2.1 添加 LangGraph 依赖项并实现类型化的 Planner、Executor、Replanner 和 Report 节点。
- [x] 2.2 实现强制性的 SOP 检索、通用计划回退、有界实数 MCP 执行以及仅证据报告。
- [x] 2.3 保留图 checkpoints 和通用诊断工具调用审计生命周期，同时生成共享的 SSE 事件。

## 3. API 和验证

- [x] 3.1 将经过身份验证的诊断流和持久化的状态/报告有效载荷接入 FastAPI。
- [x] 3.2 为 SOP/no-SOP、工具故障、tenant 隔离、SSE 和报告持久化添加聚焦的仓库、工作流和 API 测试。
- [x] 3.3 使用本地 CLS MCP 服务器启动本地项目，运行一个真实的 CLS 支持的诊断，验证、归档、提交并推送。
